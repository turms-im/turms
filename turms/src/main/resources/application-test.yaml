logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              ReactiveMongoTemplate: debug
spring:
  data:
    mongodb:
      uri: mongodb://localhost:29510/turms-test
  profiles:
    include: test-latest