// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               v5.29.1
// source: model/group/group_join_questions_answer_result.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "im.turms.proto";

export interface GroupJoinQuestionsAnswerResult {
  score: number;
  questionIds: string[];
  joined: boolean;
}

function createBaseGroupJoinQuestionsAnswerResult(): GroupJoinQuestionsAnswerResult {
  return { score: 0, questionIds: [], joined: false };
}

export const GroupJoinQuestionsAnswerResult: MessageFns<GroupJoinQuestionsAnswerResult> = {
  encode(message: GroupJoinQuestionsAnswerResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.score !== 0) {
      writer.uint32(8).int32(message.score);
    }
    writer.uint32(18).fork();
    for (const v of message.questionIds) {
      writer.int64(v);
    }
    writer.join();
    if (message.joined !== false) {
      writer.uint32(24).bool(message.joined);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupJoinQuestionsAnswerResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGroupJoinQuestionsAnswerResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.score = reader.int32();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.questionIds.push(reader.int64().toString());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.questionIds.push(reader.int64().toString());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.joined = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
}
