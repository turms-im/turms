// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               v5.29.1
// source: request/conference/update_meeting_invitation_request.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { ResponseAction } from "../../constant/response_action";
import { Value } from "../../model/common/value";

export const protobufPackage = "im.turms.proto";

export interface UpdateMeetingInvitationRequest {
  /** Query filter */
  meetingId: string;
  password?:
    | string
    | undefined;
  /** Update */
  responseAction: ResponseAction;
  customAttributes: Value[];
}

function createBaseUpdateMeetingInvitationRequest(): UpdateMeetingInvitationRequest {
  return { meetingId: "0", password: undefined, responseAction: 0, customAttributes: [] };
}

export const UpdateMeetingInvitationRequest: MessageFns<UpdateMeetingInvitationRequest> = {
  encode(message: UpdateMeetingInvitationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.meetingId !== "0") {
      writer.uint32(8).int64(message.meetingId);
    }
    if (message.password !== undefined) {
      writer.uint32(18).string(message.password);
    }
    if (message.responseAction !== 0) {
      writer.uint32(40).int32(message.responseAction);
    }
    for (const v of message.customAttributes) {
      Value.encode(v!, writer.uint32(122).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateMeetingInvitationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateMeetingInvitationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.meetingId = reader.int64().toString();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.password = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.responseAction = reader.int32() as any;
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.customAttributes.push(Value.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
}