// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.1
//   protoc               v5.27.2
// source: request/conference/query_meetings_request.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
import { Value } from "../../model/common/value";

export const protobufPackage = "im.turms.proto";

export interface QueryMeetingsRequest {
  ids: string[];
  creatorIds: string[];
  userIds: string[];
  groupIds: string[];
  creationDateStart?: string | undefined;
  creationDateEnd?: string | undefined;
  skip?: number | undefined;
  limit?: number | undefined;
  customAttributes: Value[];
}

function createBaseQueryMeetingsRequest(): QueryMeetingsRequest {
  return {
    ids: [],
    creatorIds: [],
    userIds: [],
    groupIds: [],
    creationDateStart: undefined,
    creationDateEnd: undefined,
    skip: undefined,
    limit: undefined,
    customAttributes: [],
  };
}

export const QueryMeetingsRequest = {
  encode(message: QueryMeetingsRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    writer.uint32(10).fork();
    for (const v of message.ids) {
      writer.int64(v);
    }
    writer.ldelim();
    writer.uint32(18).fork();
    for (const v of message.creatorIds) {
      writer.int64(v);
    }
    writer.ldelim();
    writer.uint32(26).fork();
    for (const v of message.userIds) {
      writer.int64(v);
    }
    writer.ldelim();
    writer.uint32(34).fork();
    for (const v of message.groupIds) {
      writer.int64(v);
    }
    writer.ldelim();
    if (message.creationDateStart !== undefined) {
      writer.uint32(40).int64(message.creationDateStart);
    }
    if (message.creationDateEnd !== undefined) {
      writer.uint32(48).int64(message.creationDateEnd);
    }
    if (message.skip !== undefined) {
      writer.uint32(80).int32(message.skip);
    }
    if (message.limit !== undefined) {
      writer.uint32(88).int32(message.limit);
    }
    for (const v of message.customAttributes) {
      Value.encode(v!, writer.uint32(122).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): QueryMeetingsRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryMeetingsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag === 8) {
            message.ids.push(longToString(reader.int64() as Long));

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.ids.push(longToString(reader.int64() as Long));
            }

            continue;
          }

          break;
        case 2:
          if (tag === 16) {
            message.creatorIds.push(longToString(reader.int64() as Long));

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.creatorIds.push(longToString(reader.int64() as Long));
            }

            continue;
          }

          break;
        case 3:
          if (tag === 24) {
            message.userIds.push(longToString(reader.int64() as Long));

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.userIds.push(longToString(reader.int64() as Long));
            }

            continue;
          }

          break;
        case 4:
          if (tag === 32) {
            message.groupIds.push(longToString(reader.int64() as Long));

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.groupIds.push(longToString(reader.int64() as Long));
            }

            continue;
          }

          break;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.creationDateStart = longToString(reader.int64() as Long);
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.creationDateEnd = longToString(reader.int64() as Long);
          continue;
        case 10:
          if (tag !== 80) {
            break;
          }

          message.skip = reader.int32();
          continue;
        case 11:
          if (tag !== 88) {
            break;
          }

          message.limit = reader.int32();
          continue;
        case 15:
          if (tag !== 122) {
            break;
          }

          message.customAttributes.push(Value.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },
};

function longToString(long: Long) {
  return long.toString();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}
