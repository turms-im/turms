// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               v5.29.1
// source: request/conference/query_meetings_request.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Value } from "../../model/common/value";

export const protobufPackage = "im.turms.proto";

export interface QueryMeetingsRequest {
  ids: string[];
  creatorIds: string[];
  userIds: string[];
  groupIds: string[];
  creationDateStart?: string | undefined;
  creationDateEnd?: string | undefined;
  skip?: number | undefined;
  limit?: number | undefined;
  customAttributes: Value[];
}

function createBaseQueryMeetingsRequest(): QueryMeetingsRequest {
  return {
    ids: [],
    creatorIds: [],
    userIds: [],
    groupIds: [],
    creationDateStart: undefined,
    creationDateEnd: undefined,
    skip: undefined,
    limit: undefined,
    customAttributes: [],
  };
}

export const QueryMeetingsRequest: MessageFns<QueryMeetingsRequest> = {
  encode(message: QueryMeetingsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.ids) {
      writer.int64(v);
    }
    writer.join();
    writer.uint32(18).fork();
    for (const v of message.creatorIds) {
      writer.int64(v);
    }
    writer.join();
    writer.uint32(26).fork();
    for (const v of message.userIds) {
      writer.int64(v);
    }
    writer.join();
    writer.uint32(34).fork();
    for (const v of message.groupIds) {
      writer.int64(v);
    }
    writer.join();
    if (message.creationDateStart !== undefined) {
      writer.uint32(40).int64(message.creationDateStart);
    }
    if (message.creationDateEnd !== undefined) {
      writer.uint32(48).int64(message.creationDateEnd);
    }
    if (message.skip !== undefined) {
      writer.uint32(80).int32(message.skip);
    }
    if (message.limit !== undefined) {
      writer.uint32(88).int32(message.limit);
    }
    for (const v of message.customAttributes) {
      Value.encode(v!, writer.uint32(122).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryMeetingsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryMeetingsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.ids.push(reader.int64().toString());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.ids.push(reader.int64().toString());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag === 16) {
            message.creatorIds.push(reader.int64().toString());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.creatorIds.push(reader.int64().toString());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag === 24) {
            message.userIds.push(reader.int64().toString());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.userIds.push(reader.int64().toString());
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag === 32) {
            message.groupIds.push(reader.int64().toString());

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.groupIds.push(reader.int64().toString());
            }

            continue;
          }

          break;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.creationDateStart = reader.int64().toString();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.creationDateEnd = reader.int64().toString();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.skip = reader.int32();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.customAttributes.push(Value.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
}