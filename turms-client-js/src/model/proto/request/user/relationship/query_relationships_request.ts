// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               v5.29.1
// source: request/user/relationship/query_relationships_request.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Value } from "../../../model/common/value";

export const protobufPackage = "im.turms.proto";

export interface QueryRelationshipsRequest {
  userIds: string[];
  blocked?: boolean | undefined;
  groupIndexes: number[];
  lastUpdatedDate?: string | undefined;
  userIdsForCommonRelationships: string[];
  customAttributes: Value[];
}

function createBaseQueryRelationshipsRequest(): QueryRelationshipsRequest {
  return {
    userIds: [],
    blocked: undefined,
    groupIndexes: [],
    lastUpdatedDate: undefined,
    userIdsForCommonRelationships: [],
    customAttributes: [],
  };
}

export const QueryRelationshipsRequest: MessageFns<QueryRelationshipsRequest> = {
  encode(message: QueryRelationshipsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.userIds) {
      writer.int64(v);
    }
    writer.join();
    if (message.blocked !== undefined) {
      writer.uint32(16).bool(message.blocked);
    }
    writer.uint32(26).fork();
    for (const v of message.groupIndexes) {
      writer.int32(v);
    }
    writer.join();
    if (message.lastUpdatedDate !== undefined) {
      writer.uint32(32).int64(message.lastUpdatedDate);
    }
    writer.uint32(42).fork();
    for (const v of message.userIdsForCommonRelationships) {
      writer.int64(v);
    }
    writer.join();
    for (const v of message.customAttributes) {
      Value.encode(v!, writer.uint32(122).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryRelationshipsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryRelationshipsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.userIds.push(reader.int64().toString());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.userIds.push(reader.int64().toString());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.blocked = reader.bool();
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.groupIndexes.push(reader.int32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.groupIndexes.push(reader.int32());
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.lastUpdatedDate = reader.int64().toString();
          continue;
        }
        case 5: {
          if (tag === 40) {
            message.userIdsForCommonRelationships.push(reader.int64().toString());

            continue;
          }

          if (tag === 42) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.userIdsForCommonRelationships.push(reader.int64().toString());
            }

            continue;
          }

          break;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.customAttributes.push(Value.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
}